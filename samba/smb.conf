[global]
   workgroup = WORKGROUP
   server string = Samba Server
   netbios name = SAMBA
   security = user

[data]
   comment = Samba Share
   path = /data
   browsable = yes
   writable = yes
   guest ok = yes
   read only = no
   valid users = admin