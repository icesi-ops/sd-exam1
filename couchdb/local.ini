[couchdb]
; Uncomment the following line to trigger basic authentication
; require_valid_user = true

[admins]
; Set the admin username and password
admin = admin

[httpd]
; Uncomment the following line to set the desired port (default is 5984)
; port = 5984

[vhosts]
; Add any virtual hosts if needed

[couch_peruser]
; Uncomment the following line to enable per-user databases
; enable = true

[log]
; Configure logging settings if needed

[query_servers]
; Configure query servers if needed

[daemons]
; Configure daemons if needed

[cors]
; Configure CORS settings if needed

[database]
; Create the specified database on CouchDB startup